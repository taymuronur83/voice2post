name: Render Video
on:
  repository_dispatch:
    types: [render-video]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm install

      - name: Render Video
        run: |
          mkdir -p public/outputs
          npx remotion render src/index.tsx MyVideo public/outputs/final-video.mp4 --force

      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          # Videoyu repoya geri yüklüyoruz ki URL üzerinden erişebilelim
          git add public/outputs/final-video.mp4
          git commit -m "Video render edildi ve yüklendi" || echo "Degisiklik yok"
          git push origin main
