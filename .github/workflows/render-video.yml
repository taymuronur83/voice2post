name: Render Video

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch: # Bu satırı silmedim, istersen hala elle de başlatabilirsin.

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Install Remotion Browser
        run: npx remotion browser install

      - name: Render Video
        run: npm run render # package.json'daki "render" komutunu tetikler

      - name: Upload Video Artifact
        uses: actions/upload-artifact@v4
        with:
          name: my-video
          path: out/video.mp4
